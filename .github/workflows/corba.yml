name: Corba

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: ls
      run: echo `ls`
    - name: pwd 
      run: echo `pwd`
    - name: goto external
      run: cd /home/runner/work/corba_crap/external
    - name: pwd 
      run: echo `pwd`
    - name: ls
      run: echo `ls`
    - name: configure
      run: ./configure
